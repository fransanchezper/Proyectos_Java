server:
  port: 8081    
              
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fransanchezper/Proyectos_Java/
          default-label: main

eureka:
  client:
    service-url:
      #defaultZone: http://eureka-server:8761/eureka
      defaultZone: http://localhost:8761/eureka